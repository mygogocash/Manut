{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["sin1"],
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install",
  "buildCommand": "cd ../.. && pnpm db:generate && pnpm --filter @nexora/types build && pnpm --filter @nexora/utils build && node scripts/bundle-api-for-vercel.mjs && pnpm turbo run build --filter=@nexora/web",
  "functions": {
    "src/pages/api/[[...path]].js": {
      "memory": 1769,
      "maxDuration": 60,
      "regions": ["sin1"],
      "includeFiles": "{src/server/**,src/generated/prisma/**}"
    }
  }
}
