/**
 * Global keyboard shortcuts overlay. Mount once inside the workspace layout.
 * Pressing `?` (Shift+/) outside any input opens a modal that reuses the
 * Settings → Keyboard shortcuts panel.
 */
export { KeyboardShortcutsOverlay } from './keyboard-shortcuts-overlay';
export { useShortcutOverlayTrigger } from './use-shortcut-overlay-trigger';
